import React from 'react';
import { Route, IndexRoute } from 'react-router';

import _Root from './components/_Root';

//list all the routes and what components should be shown
export default (
<Route path="/" component={_Root}/>
);